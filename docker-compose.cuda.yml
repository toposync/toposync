services:
  toposync:
    image: ${TOPOSYNC_CUDA_IMAGE:-ghcr.io/toposync/toposync:0.8.0-cuda}
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              capabilities: [gpu]
              count: all
