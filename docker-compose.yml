services:
  toposync:
    image: ${TOPOSYNC_IMAGE:-ghcr.io/toposync/toposync:0.7.6}
    ports:
      - "${TOPOSYNC_PORT:-8000}:8000"
    volumes:
      - ${TOPOSYNC_DATA_VOLUME:-./toposync-data}:/data
    restart: unless-stopped
