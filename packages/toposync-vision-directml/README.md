# Toposync Vision DirectML bundle

Alternative Toposync application bundle with ONNX Runtime DirectML (`onnxruntime-directml`) for Windows GPU acceleration.

This bundle is intended for Windows environments with DirectX 12 capable GPUs where you want the first-party `vision` stack to use `DmlExecutionProvider` when present.
